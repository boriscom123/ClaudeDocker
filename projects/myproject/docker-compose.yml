# myproject на этом VPS: порт наружу не публикуется — вход держит общий nginx,
# который ходит в апстрим myproject:3003 по сети claude-net.
services:
  myproject:
    ports: !reset []
    networks: !override [claude-net]

networks:
  claude-net:
    external: true
